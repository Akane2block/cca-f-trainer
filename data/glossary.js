/* CCA-F 用語集
   window.GLOSSARY … 専門用語の定義（日英）。
   各項: { key, domain, ja(日本語名), en(英語名), def(日本語解説), defEn(英語解説), aliases:[問題文照合用] }
   aliases は問題文に出てきたら自動でその問題に用語チップを付けるための照合語。 */
window.GLOSSARY = [
  /* ===== agentic ===== */
  { key:'agentic-workflow', domain:'agentic', ja:'エージェント的ワークフロー', en:'Agentic workflow',
    def:'LLMが自分でツール選択・計画・実行を繰り返して目的を達成する処理の流れ。', defEn:'A flow where the LLM plans, selects tools, and acts iteratively to reach a goal.',
    aliases:['agentic','エージェント的','自律エージェント','autonomous agent'] },
  { key:'orchestrator-worker', domain:'agentic', ja:'オーケストレーター/ワーカー', en:'Orchestrator–worker',
    def:'親（coordinator）がタスクを小タスクに分割し、子（worker/subagent）に分担させて結果を統合する構成。', defEn:'A coordinator splits work into subtasks delegated to workers/subagents, then synthesizes results.',
    aliases:['orchestrator','orchestration','coordinator','オーケストレ','コーディネータ','責務分割','分担'] },
  { key:'subagent', domain:'agentic', ja:'サブエージェント', en:'Subagent',
    def:'特定の小タスクだけを担う子エージェント。責務を分けて精度と安全性を上げる。', defEn:'A child agent responsible for a single narrow subtask, separating responsibilities.',
    aliases:['subagent','サブエージェント','子エージェント'] },
  { key:'hitl', domain:'agentic', ja:'人間承認 (HITL)', en:'Human-in-the-loop (HITL)',
    def:'高リスク・不可逆な操作の前に人間の確認/承認を挟む設計。', defEn:'Insert human review/approval before high-risk or irreversible actions.',
    aliases:['HITL','human-in-the-loop','人間確認','人間承認','人の承認','人間の承認','承認を挟','人間に引き継'] },
  { key:'least-privilege', domain:'agentic', ja:'最小権限', en:'Least privilege',
    def:'各ツール/エージェントに必要最小限の権限だけ与え、事故時の被害範囲を抑える原則。', defEn:'Grant only the minimum permissions needed, limiting blast radius on failure.',
    aliases:['最小権限','least privilege','権限を最小','権限過多','全権限','権限を絞'] },
  { key:'guardrail', domain:'agentic', ja:'ガードレール', en:'Guardrail',
    def:'権限・承認・上限・禁止など、エージェントの暴走を構造的に防ぐ制約の総称。', defEn:'Structural constraints (permissions, approvals, limits) that keep an agent from going off the rails.',
    aliases:['ガードレール','guardrail'] },
  { key:'runaway-loop', domain:'agentic', ja:'暴走ループ対策', en:'Runaway loop control',
    def:'ステップ上限・終了条件・タイムアウトを設け、超過時に停止して人へ渡す。', defEn:'Step limits, stop conditions, and timeouts that halt and hand off when exceeded.',
    aliases:['無限ループ','無限に繰り返','ステップ数の上限','step limit','タイムアウト','timeout','終了条件','暴走'] },
  { key:'critic', domain:'agentic', ja:'検証エージェント (critic)', en:'Critic / verifier agent',
    def:'生成結果を別観点で検証する役。生成側と独立した基準を持たせるのが要点。', defEn:'An agent that checks outputs from an independent perspective; independence is key.',
    aliases:['critic','検証エージェント','verifier','検証側','盲点'] },
  { key:'prompt-injection', domain:'agentic', ja:'プロンプトインジェクション', en:'Prompt injection',
    def:'取得データ等に紛れた悪意の指示にモデルが従わされる攻撃。データはデータとして扱い、重要操作は外側で制御する。', defEn:'An attack where malicious instructions hidden in data hijack the model; treat data as data and gate critical actions externally.',
    aliases:['インジェクション','injection','指示文をデータ','以前の指示を無視'] },
  { key:'blast-radius', domain:'agentic', ja:'被害範囲 (blast radius)', en:'Blast radius',
    def:'事故が起きたときに影響が及ぶ範囲。権限を絞るほど小さくできる。', defEn:'The scope of damage when something fails; smaller with tighter permissions.',
    aliases:['被害','blast radius','影響範囲'] },
  { key:'dry-run', domain:'agentic', ja:'ドライラン/サンドボックス', en:'Dry run / sandbox',
    def:'本番反映せず動作だけ確認する、または隔離環境で実行して安全を確かめる手法。', defEn:'Run without applying changes, or in an isolated environment, to verify safety first.',
    aliases:['ドライラン','dry run','サンドボックス','sandbox','隔離環境'] },

  /* ===== claudecode ===== */
  { key:'claude-md', domain:'claudecode', ja:'CLAUDE.md', en:'CLAUDE.md',
    def:'プロジェクトの方針・規約・禁止事項をリポジトリに置き、Claude Codeへ一貫して効かせる指示ファイル。', defEn:'A repo file of project guidance/standards that consistently instructs Claude Code.',
    aliases:['CLAUDE.md','claude.md'] },
  { key:'slash-command', domain:'claudecode', ja:'スラッシュコマンド/スキル', en:'Slash command / skill',
    def:'繰り返す手順を名前付きで定義し、チームで共有・再利用できる仕組み。', defEn:'Named, shareable definitions of repeatable procedures for reuse across a team.',
    aliases:['スラッシュコマンド','slash command','カスタムコマンド','スキル','skill'] },
  { key:'hooks', domain:'claudecode', ja:'hooks（フック）', en:'Hooks',
    def:'特定イベント時に決まったコマンド（lint/test等）を自動実行する仕組み。人の記憶に依存しない。', defEn:'Auto-run defined commands (lint/test, etc.) on events, independent of memory.',
    aliases:['hooks','フック','イベント時に','自動実行'] },
  { key:'permissions', domain:'claudecode', ja:'権限/許可リスト', en:'Permissions / allowlist',
    def:'settings.json等で実行可能な操作を制御し、危険な操作を制限する設定。', defEn:'Settings (e.g., settings.json) controlling allowed operations and restricting risky ones.',
    aliases:['許可リスト','allowlist','settings.json','権限設定','許可制'] },
  { key:'mcp-config', domain:'claudecode', ja:'MCP接続設定（プロジェクト共有）', en:'MCP config (project-shared)',
    def:'外部ツール/データへのMCP接続をプロジェクト設定として共有し、全員に同じ接続を効かせる。', defEn:'Share MCP connections to external tools/data as project config so everyone gets the same setup.',
    aliases:['MCP接続','接続設定','プロジェクト設定として','プロジェクト設定で共有'] },
  { key:'project-memory', domain:'claudecode', ja:'プロジェクトメモリ', en:'Project memory',
    def:'リポジトリにコミットして共有する、再現性のある設定・指示の総称（CLAUDE.md等）。', defEn:'Committed, shareable, reproducible config/instructions (e.g., CLAUDE.md).',
    aliases:['プロジェクトメモリ','コミットして共有','.claude'] },
  { key:'ci-cd', domain:'claudecode', ja:'CI/CD連携', en:'CI/CD integration',
    def:'自動チェック・レビュー・失敗時停止を開発パイプラインに組み込むこと。自動マージは危険。', defEn:'Wiring automated checks/reviews and fail-stops into the pipeline; auto-merge is risky.',
    aliases:['CI/CD','自動マージ','パイプライン','差分確認','レビュー導線'] },

  /* ===== prompt ===== */
  { key:'structured-output', domain:'prompt', ja:'構造化出力', en:'Structured output',
    def:'自由文でなく、決まった型（JSON等）で出力させ、機械処理・検証を可能にする。', defEn:'Produce output in a fixed shape (e.g., JSON) instead of free text, enabling validation.',
    aliases:['構造化出力','structured output','構造化された出力'] },
  { key:'json-schema', domain:'prompt', ja:'JSONスキーマ', en:'JSON schema',
    def:'出力の型・必須項目・制約を定義する仕様。出力の「契約」として使う。', defEn:'A spec defining output shape, required fields, and constraints; used as a contract.',
    aliases:['JSON schema','JSONスキーマ','schema','スキーマ'] },
  { key:'validation', domain:'prompt', ja:'検証 (validation)', en:'Validation',
    def:'出力がスキーマ/制約を満たすか機械的に確認するゲート。', defEn:'A gate that mechanically checks output against schema/constraints.',
    aliases:['検証','validation','validate','検証に失敗','検証ゲート'] },
  { key:'retry', domain:'prompt', ja:'リトライ (retry)', en:'Retry',
    def:'検証失敗時に再試行する。無限ではなく上限/バックオフ付きが原則。', defEn:'Re-attempt on validation failure; bounded with limits/backoff, not infinite.',
    aliases:['retry','再試行','リトライ'] },
  { key:'system-prompt', domain:'prompt', ja:'システムプロンプト', en:'System prompt',
    def:'役割・制約・トーンを固定する上位の指示。', defEn:'Top-level instruction fixing role, constraints, and tone.',
    aliases:['system prompt','システムプロンプト'] },
  { key:'few-shot', domain:'prompt', ja:'Few-shot（例示）', en:'Few-shot',
    def:'入出力の例をいくつか与えて形式・基準を学ばせる手法。', defEn:'Provide a few input/output examples to teach format and criteria.',
    aliases:['few-shot','例示','例を与え'] },
  { key:'cot', domain:'prompt', ja:'思考の連鎖 (CoT)', en:'Chain-of-thought',
    def:'段階的に推論させて精度を上げる手法。最終的な下流出力は構造化だけを渡すのが定石。', defEn:'Step-by-step reasoning to improve accuracy; pass only the structured result downstream.',
    aliases:['chain-of-thought','CoT','段階的に推論','段階的推論','思考の連鎖'] },
  { key:'xml-tags', domain:'prompt', ja:'XMLタグ', en:'XML tags',
    def:'入力・指示・データをタグで囲って構造を明示し、取り違えを防ぐ。', defEn:'Wrap input/instructions/data in tags to make structure explicit and avoid confusion.',
    aliases:['XMLタグ','XML tag','タグで囲'] },
  { key:'prompt-caching', domain:'prompt', ja:'プロンプトキャッシュ', en:'Prompt caching',
    def:'固定の長い前置きを再利用してコスト/レイテンシを下げる。可変部だけ変える。', defEn:'Reuse a fixed long prefix to cut cost/latency; vary only the dynamic part.',
    aliases:['prompt caching','プロンプトキャッシュ','キャッシュ','前置きを'] },
  { key:'temperature', domain:'prompt', ja:'temperature', en:'Temperature',
    def:'出力のランダム性を決めるパラメータ。低いほど決定的。速度/コスト最適化の手段ではない。', defEn:'A parameter for output randomness; lower is more deterministic. Not a speed/cost lever.',
    aliases:['temperature'] },
  { key:'hallucination', domain:'prompt', ja:'ハルシネーション', en:'Hallucination',
    def:'モデルが事実でない内容をもっともらしく生成すること。grounding/検証で抑える。', defEn:'The model plausibly generating false content; mitigate with grounding/validation.',
    aliases:['ハルシネーション','hallucination','作り話','でっち上げ'] },
  { key:'grounding', domain:'prompt', ja:'グラウンディング', en:'Grounding',
    def:'回答を信頼できる根拠（取得データ・出典）に結びつけること。', defEn:'Tying answers to trustworthy sources/retrieved data.',
    aliases:['grounding','グラウンディング','出典','根拠に基づ'] },

  /* ===== tool ===== */
  { key:'mcp', domain:'tool', ja:'MCP（Model Context Protocol）', en:'Model Context Protocol (MCP)',
    def:'Claude等と外部ツール/データを標準プロトコルで接続する仕組み。複数クライアントから再利用できる。', defEn:'An open protocol to connect Claude to external tools/data, reusable across clients.',
    aliases:['MCP','Model Context Protocol','MCPサーバー','MCP server'] },
  { key:'tool-use', domain:'tool', ja:'ツール利用 (tool use)', en:'Tool use / function calling',
    def:'モデルがツールを呼び、結果を受け取って続行する仕組み（tool_use/tool_result）。', defEn:'The model calls tools and continues with their results (tool_use/tool_result).',
    aliases:['tool use','function calling','ツール呼び出し','ツールを呼','tool_use'] },
  { key:'tool-schema', domain:'tool', ja:'ツールスキーマ/description', en:'Tool schema / description',
    def:'いつ使うか・各引数の意味と制約・入出力を明確に書いた定義。誤呼び出しを減らす契約。', defEn:'A definition stating when to use the tool, argument meaning/constraints, and I/O; a contract that reduces misfires.',
    aliases:['description','ツールスキーマ','tool schema','引数の意味','スキーマに'] },
  { key:'single-responsibility', domain:'tool', ja:'単一責務（巨大ツール回避）', en:'Single responsibility',
    def:'1ツール1責務に分け、何でもできる巨大ツールを避けて曖昧さと誤用を防ぐ。', defEn:'One tool, one job; avoid do-everything giant tools that breed ambiguity and misuse.',
    aliases:['単一責務','巨大ツール','1つの巨大','責務過多','限定ツール','限定したツール'] },
  { key:'idempotency', domain:'tool', ja:'冪等性 (idempotency)', en:'Idempotency',
    def:'同じ操作を複数回実行しても結果が壊れない性質。リトライや重複実行に耐える設計。', defEn:'Repeating the same operation does not corrupt results; safe under retries/duplicates.',
    aliases:['冪等','idempoten'] },
  { key:'structured-error', domain:'tool', ja:'構造化エラー応答', en:'Structured error response',
    def:'ツールが失敗を握りつぶさず、モデルが回復できる形でエラーを返すこと。', defEn:'Tools return errors in a form the model can recover from, instead of swallowing them.',
    aliases:['構造化エラー','structured error','エラーを返','握りつぶ'] },
  { key:'secrets', domain:'tool', ja:'認証情報/シークレット管理', en:'Credential / secret management',
    def:'トークン等をプロンプトや定義に直書きせず、環境変数/秘密管理に置き参照する。', defEn:'Keep tokens out of prompts/definitions; store in env vars/secret managers and reference them.',
    aliases:['認証情報','シークレット','secret','トークン','credential','環境変数'] },
  { key:'input-validation', domain:'tool', ja:'入力検証（ツール側）', en:'Server-side input validation',
    def:'モデル出力を鵜呑みにせず、ツール側で型・範囲・単位を検証してから実行する。', defEn:'Validate type/range/units on the tool side before acting, not trusting model output blindly.',
    aliases:['入力検証','入力を検証','ツール側で検証','無検証'] },

  /* ===== context ===== */
  { key:'context-window', domain:'context', ja:'コンテキストウィンドウ', en:'Context window',
    def:'1回の推論で扱えるトークンの上限。超えると古い情報が失われる。', defEn:'The token limit for a single inference; exceeding it loses earlier information.',
    aliases:['コンテキスト上限','context window','コンテキストウィンドウ','上限に近づく'] },
  { key:'compaction', domain:'context', ja:'圧縮/要約 (compaction)', en:'Compaction / summarization',
    def:'重要な制約・決定を要約保持し、不要詳細を圧縮して文脈と精度を保つ。', defEn:'Keep key constraints/decisions as summaries and compact unneeded detail to preserve context.',
    aliases:['compaction','圧縮','要約して保持','要約保持','要約'] },
  { key:'retrieval', domain:'context', ja:'検索拡張 (RAG/retrieval)', en:'Retrieval / RAG',
    def:'全文投入せず、必要な部分だけ取得して文脈に入れる手法。', defEn:'Fetch only the needed parts into context instead of dumping everything.',
    aliases:['retrieval','RAG','検索拡張','必要な部分だけ取得','必要分だけ'] },
  { key:'backoff', domain:'context', ja:'指数バックオフ', en:'Exponential backoff',
    def:'リトライ間隔を徐々に広げて相手への負荷と暴走を防ぐ。上限付き有限リトライとセット。', defEn:'Progressively widen retry intervals to avoid hammering and runaway; paired with bounded retries.',
    aliases:['バックオフ','backoff','指数バックオフ','有限リトライ'] },
  { key:'fallback', domain:'context', ja:'フォールバック', en:'Fallback',
    def:'失敗時の代替手段（縮退・人間通知・代替処理）。不確実な結果はそのまま採用しない。', defEn:'A backup path on failure (degrade/notify/alternative); never accept uncertain results as-is.',
    aliases:['フォールバック','fallback','代替手段','人間通知'] },
  { key:'graceful-degradation', domain:'context', ja:'縮退運転', en:'Graceful degradation',
    def:'依存部分が落ちてもコア機能は動かし続け、影響を局所化する。', defEn:'Keep core features running when a dependency fails, localizing impact.',
    aliases:['縮退','graceful degradation','縮退運転','コア機能は'] },
  { key:'circuit-breaker', domain:'context', ja:'サーキットブレーカー', en:'Circuit breaker',
    def:'失敗が続く依存先への呼び出しを一時遮断し、連鎖障害を防ぐ。', defEn:'Temporarily cut off calls to a failing dependency to prevent cascading failures.',
    aliases:['サーキットブレーカー','circuit breaker'] },
  { key:'observability', domain:'context', ja:'観測性 (logging/monitoring)', en:'Observability',
    def:'ログ・監視で失敗を検知できる状態にすること。握りつぶしの逆。', defEn:'Logging/monitoring so failures are detectable; the opposite of swallowing errors.',
    aliases:['観測性','observability','logging','ログ','monitoring','監視'] },
  { key:'handoff', domain:'context', ja:'引き継ぎ (handoff)', en:'Handoff',
    def:'目的・確定事項・制約・現在地・残課題・次アクションを構造化して引き渡す。', defEn:'Hand off a structured summary: goal, decisions, constraints, status, open items, next actions.',
    aliases:['引き継ぎ','handoff','引き継ぎサマリー'] },
  { key:'checkpoint', domain:'context', ja:'チェックポイント/状態永続化', en:'Checkpoint / state persistence',
    def:'途中状態を保存し、リセットや失敗後に再開できるようにする。', defEn:'Persist intermediate state so work can resume after resets or failures.',
    aliases:['チェックポイント','checkpoint','状態を永続','状態の永続','永続化'] },

  /* ===== 上級（評価指標・分散処理など横断語） ===== */
  { key:'precision', domain:'prompt', ja:'適合率 (Precision)', en:'Precision',
    def:'抽出/判定した中で本当に正しかった割合。上げると過剰検出（偽陽性）が減る。基準の明確化が効く。', defEn:'Of the items flagged, the fraction that were truly correct; raising it cuts false positives.',
    aliases:['precision','適合率'] },
  { key:'recall', domain:'prompt', ja:'再現率 (Recall)', en:'Recall',
    def:'本来拾うべきもののうち実際に拾えた割合。広く拾うほど上がるが偽陽性も増えがち。Precisionと綱引き。', defEn:'Of all items that should be caught, the fraction actually caught; broad capture raises it but adds false positives.',
    aliases:['recall','再現率'] },
  { key:'false-positive', domain:'prompt', ja:'偽陽性 (false positive)', en:'False positive',
    def:'実際は該当しないのに「該当」と誤検出すること。過剰抽出・過剰アラートの正体。', defEn:'Flagging something as a match when it is not; the cause of over-extraction and noisy alerts.',
    aliases:['偽陽性','false positive','過剰抽出','過剰な指摘','過剰検出','偽陰性'] },
  { key:'calibration', domain:'prompt', ja:'校正 (Calibration)', en:'Calibration',
    def:'モデルの「確信度」が実際の正答率と一致している度合い。LLMの自己採点スコアは校正されておらず、閾値で切っても確実に効かない。', defEn:'How well a model\'s stated confidence matches real accuracy; LLM self-scores are poorly calibrated, so thresholding them is unreliable.',
    aliases:['calibration','校正','確信度','自己採点','自己評価スコア','confidence'] },
  { key:'eval-set', domain:'prompt', ja:'評価セット (eval)', en:'Evaluation set',
    def:'プロンプトや設計の変更が良くなったかを測るための、正解付きテストデータ。感覚でなく数値で判断する。', defEn:'Labeled test data to measure whether a prompt/design change actually improved things; decide by metrics, not vibes.',
    aliases:['eval','評価セット','ゴールデンセット','評価データ'] },
  { key:'fine-tuning', domain:'prompt', ja:'ファインチューニング', en:'Fine-tuning',
    def:'モデル自体を追加学習で調整する手法。多くの設計問題はプロンプト/構造/ツールで解け、安易な万能策ではない。', defEn:'Adapting the model via extra training; many design problems are solvable with prompts/structure/tools, so it is not a default fix.',
    aliases:['ファインチューニング','fine-tuning','fine tune'] },
  { key:'routing', domain:'agentic', ja:'ルーティング/ルーター', en:'Routing / router',
    def:'入力の種類・条件に応じて適切な専門処理（サブエージェント）へ振り分ける構成。分岐の多いタスクの定石。', defEn:'Directing each input to the right specialized handler/subagent by type/condition; the go-to for highly branched tasks.',
    aliases:['ルーティング','ルーター','routing','router','振り分け','動的に振り分け','全部並列に呼','必要なエージェントだけ','コーディネーターが入力を分析'] },
  { key:'ensemble', domain:'agentic', ja:'アンサンブル/多数決', en:'Ensemble / majority vote',
    def:'複数の出力を統合（多数決等）して精度や安定性を上げる手法。手順が異なるタスクには無意味になり得る。', defEn:'Combining multiple outputs (e.g., majority vote) for accuracy/stability; meaningless when tasks need different procedures.',
    aliases:['アンサンブル','ensemble','多数決','majority vote','投票'] },
  { key:'mapreduce', domain:'agentic', ja:'MapReduce/並列分割', en:'MapReduce / parallel split',
    def:'独立な部分問題を並列処理して集約する手法。順序依存・ネスト構造の処理には不適。', defEn:'Process independent subproblems in parallel and aggregate; ill-suited to order-dependent or nested processing.',
    aliases:['MapReduce','map reduce','並列処理','並列に','スループット','throughput'] },
  { key:'saga', domain:'agentic', ja:'補償 (Saga/compensation)', en:'Saga / compensation',
    def:'複数ステップの一部が失敗したとき、すでに完了した操作を取り消す補償処理で整合性を保つ。', defEn:'When some steps of a multi-step action fail, undo already-done operations via compensating actions to keep consistency.',
    aliases:['saga','compensation','補償','部分失敗','ロールバック'] },
  { key:'sla', domain:'context', ja:'SLA/レイテンシ予算', en:'SLA / latency budget',
    def:'応答時間や可用性の目標。これを守りつつ精度・コストを両立させる設計判断が問われる。', defEn:'Targets for response time/availability; designs must meet them while balancing accuracy and cost.',
    aliases:['SLA','レイテンシ','latency','応答時間'] },
  { key:'jitter', domain:'context', ja:'ジッター (jitter)', en:'Jitter',
    def:'リトライ間隔に乱数を加え、再試行が同時集中（thundering herd）するのを防ぐ。backoffとセット。', defEn:'Add randomness to retry intervals to avoid synchronized retry storms (thundering herd); paired with backoff.',
    aliases:['jitter','ジッター'] },
  { key:'payload', domain:'tool', ja:'ペイロード (payload)', en:'Payload',
    def:'処理・送受信される実データ本体。サイズが大きいとコンテキストを圧迫するため抜粋/分割が要る。', defEn:'The actual data being processed/transferred; large payloads bloat context, so excerpt or paginate.',
    aliases:['ペイロード','payload'] },
  { key:'pipeline', domain:'tool', ja:'パイプライン', en:'Pipeline',
    def:'処理を段階的につなげた流れ。固定パイプラインは分岐の多い入力に弱く、ルーティング等で補う。', defEn:'A staged processing flow; fixed pipelines struggle with highly branched inputs and need routing.',
    aliases:['パイプライン','pipeline','固定の処理'] },
  /* ===== 学習記録から追加（2026-06） ===== */
  { key:'message-batches', domain:'context', ja:'Message Batches API（メッセージバッチ）', en:'Message Batches API',
    def:'大量リクエストをまとめて非同期処理し、レイテンシと引き換えにコストを約50%オフにする仕組み。1バッチ最大10万件/256MB・処理は最大24時間・結果は29日保持。Bedrock/Vertexは非対応。即時応答が要る用途には使わない。', defEn:'Asynchronously processes large batches at ~50% lower cost in exchange for latency. Up to 100k requests/256MB per batch, max 24h processing, results kept 29 days; not supported on Bedrock/Vertex. Not for real-time needs.',
    aliases:['バッチAPI','message batches','batches','バッチ処理','非同期処理','batch api'] },
  { key:'custom-id', domain:'context', ja:'custom_id（バッチ識別子）', en:'custom_id',
    def:'Message Batches APIで各リクエストに付ける自分用の識別子。結果は順不同で返るため、位置（インデックス）ではなく custom_id（主キー推奨）で元レコードに突合する。', defEn:'A caller-defined identifier on each Batch API request. Results return out of order, so map them back by custom_id (use your primary key), never by position/index.',
    aliases:['custom_id','カスタムID','識別子','主キーを','順不同'] },
  { key:'strict-tool-use', domain:'tool', ja:'厳密ツール使用（strict）', en:'Strict tool use',
    def:'ツール定義に strict: true を付け、Claudeの tool_use.input が input_schema に厳密一致することを保証する機能。必須引数の欠落・型ズレを防ぐ。additionalProperties: false と required が必要。strict はツール定義側に置く（tool_choice では効かない）。', defEn:'Setting strict: true on a tool definition guarantees Claude\'s tool_use.input strictly matches input_schema, preventing missing required args or type drift. Needs additionalProperties: false and required. Put strict on the tool definition, not tool_choice.',
    aliases:['strict','strict: true','厳密ツール','必須引数','required','input_schema'] },
  { key:'silent-failure', domain:'tool', ja:'サイレント・フェイラー（isError）', en:'Silent failure (isError)',
    def:'ツールが本当は失敗しているのにエラーを握りつぶし「成功・0件」として返すアンチパターン。Claudeは isError: true が来ない限り疑わず、障害をデータ不在と誤解する。失敗は isError: true で伝えるのが正解。MCPプロトコルは検知せず運ぶだけ。', defEn:'An anti-pattern where a tool swallows an error and returns "success / empty" though it actually failed. Claude trusts it unless isError: true is set, mistaking failures for absence of data. Surface failures with isError: true; the MCP protocol only transports and will not detect it.',
    aliases:['サイレント','silent failure','isError','握りつぶ','例外をキャッチ','空の結果'] },
  { key:'rpa', domain:'agentic', ja:'RPA（ロボティック・プロセス・オートメーション）', en:'RPA (Robotic Process Automation)',
    def:'人のPC定型操作（クリック・入力・転記）を画面操作の模倣で自動化する従来型。ルールベースで判断・推論はせず、UI変更に弱い。定型はRPA、非定型・判断ありはLLM/エージェント、という適材適所が問われる。', defEn:'Traditional automation that mimics human GUI actions (clicks, typing, copying). Rule-based with no reasoning and brittle to UI changes. Fit-for-purpose is tested: RPA for fixed routines, LLM/agents for non-routine, judgment-heavy tasks.',
    aliases:['RPA','ロボティック','定型作業','ルールベース','画面操作'] },
  { key:'bigquery', domain:'context', ja:'BigQuery', en:'BigQuery',
    def:'Google Cloudのサーバーレスなデータウェアハウス。ペタバイト級をSQLで高速集計。Claude（LLM）の機能ではなく周辺のデータ基盤。大量データを全部プロンプトに渡すのではなく、集計はBigQueryで行い要点だけをClaudeに渡すのが正しい役割分担。', defEn:'Google Cloud\'s serverless data warehouse that aggregates petabyte-scale data via SQL. It is data infrastructure, not an LLM feature. Don\'t dump all data into the prompt—aggregate in BigQuery and pass only the essentials to Claude.',
    aliases:['BigQuery','データウェアハウス','data warehouse','データ基盤'] },
  { key:'regex', domain:'claudecode', ja:'正規表現（regex）', en:'Regular expression (regex)',
    def:'文字列パターンを細かく指定する記法（| で「または」、\\. で「ドットそのもの」など）。Claude Codeのルール適用対象 paths は正規表現ではなくグロブを使うため、正規表現風の書き方は誤り。', defEn:'A notation for fine-grained string patterns (| for "or", \\. for a literal dot). Claude Code rule paths use glob, not regex, so regex-style patterns are wrong there.',
    aliases:['正規表現','regex','regular expression'] },
  { key:'glob', domain:'claudecode', ja:'グロブパターン（glob）', en:'Glob pattern',
    def:'ファイルパスをざっくり指定する簡単なパターン（* は任意のファイル名、** は任意の階層）。Claude Codeの .claude/rules/*.md の paths にはグロブ配列を指定する。複数対象は paths: ["**/*.old.py", "**/*.service.go"] のように並べる。', defEn:'A simple file-path pattern (* = any filename, ** = any depth). Claude Code rule paths take a glob array; list multiple targets like paths: ["**/*.old.py", "**/*.service.go"].',
    aliases:['グロブ','glob','glob pattern','ワイルドカード'] },
  { key:'cc-paths', domain:'claudecode', ja:'ルール適用対象 paths（フロントマター）', en:'Rule paths (frontmatter)',
    def:'.claude/rules/*.md のYAMLフロントマターで、そのルールを効かせる対象ファイルをグロブ配列で指定するキー。1ファイルで複数パターンを管理でき、ファイル名で対象拡張子が決まるわけではない。', defEn:'A YAML-frontmatter key in .claude/rules/*.md specifying, as a glob array, which files the rule applies to. One file can manage multiple patterns; the filename does not auto-determine target extensions.',
    aliases:['paths:','フロントマター','frontmatter','ルール適用対象','.claude/rules'] },
  /* ===== 学習記録から追加（2026-07-03） ===== */
  { key:'latency', domain:'context', ja:'レイテンシ（latency）', en:'Latency',
    def:'リクエストを投げてから応答が返るまでの待ち時間。処理量ではない。Batch APIはレイテンシを犠牲にコスト約50%オフにする仕組みで、即時応答が要る用途には通常のMessages APIを使う、という設計判断が問われる。', defEn:'The wait time from sending a request to receiving the response—not throughput. The Batch API trades latency for ~50% cost savings; real-time use cases need the regular Messages API instead.',
    aliases:['レイテンシ','latency','待ち時間','応答が返るまで'] },
  { key:'endpoint', domain:'tool', ja:'エンドポイント（endpoint）', en:'Endpoint',
    def:'APIにアクセスする窓口となるURL＋HTTPメソッドの組。POST /v1/messages（同期・即時）と POST /v1/messages/batches（非同期バッチ）のように用途で叩く先が変わる。同じパスでもGET=取得/POST=作成と、メソッドで操作が変わる点も見る。', defEn:'The URL + HTTP method pair serving as an API’s entry point, e.g. POST /v1/messages (synchronous) vs POST /v1/messages/batches (async batch). The same path means different operations per method (GET fetches, POST creates).',
    aliases:['エンドポイント','endpoint','/v1/messages','/api/v2/orders'] },
  { key:'protocol', domain:'tool', ja:'プロトコル（protocol）', en:'Protocol',
    def:'システム同士がやり取りする際の共通の約束事（何を・どんな形で・どの順で送るか）。MCPプロトコルはメッセージを運ぶルールであって中身の真偽は検査しない。「プロトコルが認証失敗を自動検知して警告する」は誤り。', defEn:'The agreed rules for how systems exchange messages (what, in which format, in what order). The MCP protocol only transports messages and never inspects their correctness—“the protocol auto-detects auth failures” is false.',
    aliases:['プロトコル','protocol','共通ルール','約束事'] },
  { key:'yaml-frontmatter', domain:'claudecode', ja:'YAMLフロントマター', en:'YAML frontmatter',
    def:'Markdownファイル先頭の --- で囲む設定部分。.claude/rules/*.md ではここに paths を書き、globに一致するファイルを扱うときだけルールを読み込ませる。独自の .yaml 設定ファイルを作るのではなく、Markdownルールのフロントマターを使うのが正解。', defEn:'The settings block wrapped in --- at the top of a Markdown file. In .claude/rules/*.md it holds the paths key so a rule loads only for matching files; use it rather than inventing a separate custom YAML config file.',
    aliases:['YAMLフロントマター','frontmatter','フロントマター','YAML'] },
  { key:'symlink', domain:'claudecode', ja:'シンボリックリンク（symlink）', en:'Symbolic link (symlink)',
    def:'ファイルを別の場所から参照するためのリンク（ショートカット）。同じファイルを指して重複を減らせるが、「いつルールを読むか」の条件制御はできない。ルールの適用条件を絞りたいなら symlink ではなく .claude/rules の paths が本筋。', defEn:'A link referencing a file from another location, like a shortcut. It deduplicates files but cannot control when a rule loads; scoping rules is the job of the paths frontmatter in .claude/rules, not symlinks.',
    aliases:['シンボリックリンク','symlink','symbolic link','シムリンク'] },
  { key:'sql-injection', domain:'agentic', ja:'SQLインジェクション', en:'SQL injection',
    def:'ユーザー入力をSQL文に文字列結合し、データのつもりの入力が命令に化ける脆弱性。LLMが生成したSQL全文を無検証で実行する設計も同罪。ツール側をパラメータ化テンプレSQLに固定してClaudeには値だけ渡させ、DBアカウントは最小権限にする。', defEn:'A vulnerability where user input concatenated into SQL turns data into commands. Executing LLM-generated raw SQL unchecked is the same trap: fix parameterized template SQL on the tool side, let Claude pass only values, and keep DB accounts least-privileged.',
    aliases:['SQLインジェクション','SQL injection','パラメータ化','文字列結合','DROP TABLE'] },
  { key:'rubric', domain:'prompt', ja:'ルーブリック（Rubric）', en:'Rubric',
    def:'判定・採点のための明示的な基準。「慎重に」「保守的に」と形容詞で頼むより、「外部入力由来か」「未洗浄か」など観察可能な条件に落とす方が再現性が高く、誤検知も安定して減る。ルーブリックが曖昧なままだとfew-shot例だけでは境界条件でぶれる。', defEn:'An explicit set of judging criteria. Observable conditions (“is it external input?” “is it unsanitized?”) beat vague adjectives like “be careful” for reproducibility and reliably cut false positives; without a rubric, few-shot alone wobbles at boundaries.',
    aliases:['ルーブリック','rubric','判定基準','明示的な基準','採点基準'] },
  { key:'packet-analysis', domain:'context', ja:'パケット解析', en:'Packet analysis',
    def:'ネットワークを流れるパケットを見て「どこに何を送り、どこで失敗しているか」を調べること（Wireshark・tcpdump等）。API接続・遅延など通信原因の切り分けに使う。出力品質や誤検知など推論の問題には直接効かない（引っかけ）。', defEn:'Inspecting network packets (Wireshark, tcpdump) to see what goes where and where it fails. Good for connectivity/latency triage, but useless against inference-quality problems like wrong outputs or false positives—a common trap.',
    aliases:['パケット解析','packet analysis','Wireshark','tcpdump'] },
  { key:'packet-loss', domain:'context', ja:'パケットロス', en:'Packet loss',
    def:'通信中にパケットが途中で失われること。API呼び出しの不安定・タイムアウト・遅延の原因候補で、対策はリトライ・タイムアウト設計・バックオフ。通信品質の問題であって、モデルの回答内容や推論精度を改善する設定ではない。', defEn:'Packets lost in transit—a cause of flaky API calls, timeouts, and latency, mitigated by retries, timeout design, and backoff. A network-quality issue, not a knob for model accuracy or answer content.',
    aliases:['パケットロス','packet loss','パケットが失われ'] },
  { key:'routing-table', domain:'context', ja:'ルーティングテーブル', en:'Routing table',
    def:'「この宛先へは次にどこへ送るか」を決める経路表。社内ネットワークから外部APIへ出られない、VPC/PrivateLink/VPN経由の接続が失敗する、という通信経路の文脈で出る。認証エラーやプロンプトの問題をこれで直すわけではない。', defEn:'The table deciding the next hop per destination. It appears when traffic cannot reach an external API via VPC/PrivateLink/VPN paths; it does not fix auth errors or prompt problems.',
    aliases:['ルーティングテーブル','routing table','経路表'] },
  { key:'enum-validation', domain:'tool', ja:'enum検証', en:'Enum validation',
    def:'入力値が「選んでよい値の一覧」に入っているか確認し、外れ値を弾くこと。「この3つから選んで」とプロンプトで言うだけより、スキーマやツール引数側でenumとして制約する方が堅い。想定外の値を受け付けない防御にもなる。', defEn:'Checking that a value belongs to a fixed allowed list and rejecting the rest. Constraining via schema/tool-argument enums is far sturdier than merely asking “pick one of these three” in the prompt, and it blocks unexpected values.',
    aliases:['enum','列挙','許可された値','enum検証'] },
  { key:'json-payload', domain:'tool', ja:'JSONペイロード', en:'JSON payload',
    def:'APIリクエスト/レスポンスで運ぶJSON形式のデータ本体。JSONは「形」であって中身の安全は保証しない。ペイロード内の外部入力が未洗浄のままSQL等に結合されると危険で、検証・サニタイズ・権限チェックが必要。', defEn:'The JSON body carried in an API request/response. JSON guarantees shape, not safety—external input inside a payload still needs validation, sanitization, and permission checks before reaching SQL or other sinks.',
    aliases:['JSONペイロード','JSON payload','ペイロード内','JSON形式のデータ'] },
  { key:'delimiter', domain:'prompt', ja:'デリミタ（区切り文字）', en:'Delimiter',
    def:'データの区切りを示す文字（カンマ・タブ・---等）。プロンプトで指示とデータの境界を伝えるのに使うが、ユーザー入力に同じデリミタが含まれると境界が崩れる。安全にはXMLタグ・JSON構造・エスケープ・スキーマ検証で境界を明確化する。', defEn:'A character marking data boundaries (comma, tab, ---). Handy for separating instructions from data in prompts, but it breaks if user input contains the same delimiter; XML tags, JSON structure, escaping, or schema validation make boundaries robust.',
    aliases:['デリミタ','delimiter','区切り文字','区切り記号'] },
  { key:'edge-case', domain:'prompt', ja:'エッジケース', en:'Edge case',
    def:'空文字・超長文・区切り文字を含む値・最大最小値など、境界ぎりぎりで想定漏れしやすい例外的入力。正常系だけのfew-shot例はエッジケースに弱いので、テストやプロンプトに代表的なエッジケースも含める。', defEn:'Exceptional inputs near the boundaries—empty strings, huge text, values containing the delimiter, min/max—that normal-case designs miss. Happy-path-only few-shot examples are fragile; include representative edge cases in tests and prompts.',
    aliases:['エッジケース','edge case','境界ケース','例外的な入力'] },
  { key:'middleware-proxy', domain:'tool', ja:'ミドルウェアプロキシ', en:'Middleware proxy',
    def:'クライアントと本命APIの間に置く中継層。APIキーをフロントへ渡さない・利用者認証・ログ・レート制限・入力検証・PIIマスキングを一括で担う。置くだけで推論品質が上がるわけではなく、安全性・制御・監査のためのアーキテクチャ部品。', defEn:'An intermediary layer between clients and the target API that centralizes auth, logging, rate limiting, input validation, and PII masking while keeping API keys off the frontend. It buys control and auditability—not inference quality.',
    aliases:['ミドルウェアプロキシ','middleware proxy','プロキシ','中継サーバー','proxy'] },
  { key:'pretoolcall-hook', domain:'tool', ja:'PreToolCallフック', en:'Pre-tool-call hook',
    def:'ツール実行の直前に必ず通す関所。破壊的操作への人間承認要求、引数のスキーマ/enum検証、DROP/DELETEのブロック、許可リスト外アクセスの拒否などを行う。推論品質の改善ではなく、実行制御・監査・ガードレールの仕組み。', defEn:'A checkpoint run just before a tool executes: require human approval for destructive actions, validate arguments against schemas/enums, block DROP/DELETE, deny non-allowlisted access. Execution control (a guardrail), not a reasoning improvement.',
    aliases:['PreToolCall','pre-tool-call','ツール実行前','実行前フック','PreToolUse'] },
  { key:'posttooluse-hook', domain:'tool', ja:'PostToolUseフック', en:'Post-tool-use hook',
    def:'ツール実行後、結果をモデルへ返す前の整形所。ばらばらなレスポンスの統一スキーマへの正規化・PIIマスク・巨大結果の要約などを行う。実行してよいかを事前に止めるのはPreToolCall側の役割で、両者を混同させる引っかけが出る。', defEn:'A post-execution stage shaping tool results before the model sees them: normalize mixed response formats into one schema, mask PII, summarize huge outputs. Blocking execution beforehand is the pre-tool-call hook’s job—a classic mix-up trap.',
    aliases:['PostToolUse','post-tool-use','ツール実行後','結果を整形','統一スキーマ'] },
  { key:'disambiguation', domain:'prompt', ja:'曖昧性解消（Disambiguation）', en:'Disambiguation',
    def:'複数の意味に取れる入力を、文脈や追加質問で1つに絞ること。曖昧なまま決め打ちすると誤ツール呼び出し・誤分類につながる。確信できないときは確認するか候補と根拠を示すのが正解で、「推測で進める」は誤り。', defEn:'Narrowing an ambiguous input to one meaning via context or follow-up questions. Guessing forward causes wrong tool calls and misclassification; when unsure, confirm or present candidates with reasoning.',
    aliases:['曖昧性解消','disambiguation','曖昧さ','複数の意味に取れ'] },
  { key:'iac', domain:'context', ja:'IaC（Infrastructure as Code）', en:'Infrastructure as Code (IaC)',
    def:'サーバー・ネットワーク・IAM権限などのクラウド設定を、管理画面の手作業でなくコード/テンプレート（Terraform・CloudFormation等）で定義すること。再現可能・レビュー可能になり設定漏れが減る。プロンプトを良くする仕組みではない。', defEn:'Defining cloud configuration (servers, networking, IAM) as code/templates (Terraform, CloudFormation) instead of console clicks, making it reproducible and reviewable. It hardens operations—it does not improve prompts.',
    aliases:['Infrastructure as Code','Terraform','CloudFormation','クラウド設定テンプレート','IaC'] },
  { key:'parallelization', domain:'context', ja:'並列化（Parallelization）', en:'Parallelization',
    def:'独立した複数の処理を同時に進めて全体時間を短縮すること。大量レビュー・チャンク要約・Batch API・Map-Reduceと相性が良い。結果の順序は保証されないためcustom_id等で突合し、前の結果に依存する処理・順序が重要な処理には向かない。', defEn:'Running independent tasks concurrently to cut total time; a natural fit for bulk review, chunk summarization, the Batch API, and Map-Reduce. Order is not guaranteed (map results back via custom_id), and it does not suit order-dependent work.',
    aliases:['並列化','parallelization','同時に処理','同時に進め'] },
  { key:'uuid', domain:'agentic', ja:'UUID', en:'UUID (Universally Unique Identifier)',
    def:'世界中でほぼ重複しないよう作られる一意な識別子。並列・非同期処理では結果の順番に頼らずIDで元データと突合する。意味を持たない機械的なIDであり、アクセス制御なしに「IDを知っている＝見てよい」とする設計は危険。', defEn:'A practically collision-free unique identifier. In parallel/async processing, map results back by ID rather than order. It carries no meaning, and granting access merely because someone knows an ID—without access control—is unsafe.',
    aliases:['UUID','一意な識別子','ユニークID','identifier'] },
  { key:'tool-use-id', domain:'tool', ja:'tool_use_id', en:'tool_use_id',
    def:'tool_resultがどのtool_useへの結果かを示すID。直前の対応するtool_use.idと完全一致が必要で、新しいUUIDを作って入れると400 Bad Requestになる。ツール名では自動対応づけされない（並列呼び出しで同名ツールが複数回呼ばれ得るため）。', defEn:'The ID linking a tool_result to its tool_use. It must exactly match the preceding tool_use.id—inventing a fresh UUID yields 400 Bad Request—and tool names never substitute for it, since parallel calls can invoke the same tool repeatedly.',
    aliases:['tool_use_id','tool_use.id','ツール呼び出しのID','tool_result'] },
  { key:'confidence-score', domain:'context', ja:'確信度スコア', en:'Confidence score',
    def:'モデルが自分の出力をどれくらい正しそうと見ているかの数値。低確信度だけ人間レビューに回すHITL設計で頻出。ただし高確信度でも間違うため、高確信度グループも継続的にQC（サンプリングレビュー）する必要がある。', defEn:'A number expressing how correct the model believes its output is, common in HITL designs that route low-confidence items to humans. High-confidence outputs still fail, so the high-confidence group also needs ongoing QC sampling.',
    aliases:['確信度スコア','confidence score','低確信度','高確信度'] },
  { key:'quality-control', domain:'context', ja:'QC（品質管理）', en:'Quality control (QC)',
    def:'システムの出力が品質を保っているか継続的に確認・改善すること。低確信度だけでなく、高確信度で自動処理されたデータも一部レビューして監視するのが要点。目的はサイレント・フェイラーやドリフトの早期発見。', defEn:'Continuously verifying and improving output quality. The key move is reviewing a sample of even the high-confidence, auto-processed data—not just low-confidence items—to catch silent failures and drift early.',
    aliases:['品質管理','quality control','QCサンプリング','QCプロセス','継続的に監視'] },
  { key:'sampling', domain:'context', ja:'サンプリング（標本抽出）', en:'Sampling',
    def:'全件ではなく一部のデータを抜き出して確認・分析すること。全件レビューが重いとき、代表的な一部を人間がレビューして品質を推定する。どこから抽出するかが肝で、高確信度データの監視が目的なら高確信度グループから抜く。', defEn:'Reviewing a subset instead of everything to estimate quality when full review is too costly. Where you draw from matters: to monitor high-confidence data, sample from the high-confidence group itself.',
    aliases:['サンプリング','sampling','標本抽出','一部を抜き出'] },
  { key:'stratified-sampling', domain:'context', ja:'層化ランダム抽出', en:'Stratified random sampling',
    def:'データを属性ごとの層（大病院/小病院・単純/複雑な請求等）に分け、各層からランダムに抜き出す方法。全体から雑に抽出したり境界スコアだけ見るより、属性ごとのエラーパターンを偏りなく見つけられる。高確信度データの品質監視の定番解。', defEn:'Splitting data into strata by attributes and sampling randomly within each. It surfaces per-segment error patterns without bias—better than naive whole-population sampling or boundary-score-only checks—and is the go-to for monitoring high-confidence outputs.',
    aliases:['層化ランダム','stratified','層化抽出','層に分け'] },
  { key:'drift', domain:'context', ja:'ドリフト（Drift）', en:'Drift',
    def:'時間とともに入力データや業務パターンが変わり、以前はうまく動いていたAIの精度が落ちること（新フォーマット・新しい取引先等）。初期精度だけでなく継続監視が必要で、高確信度データのサンプリングレビューが早期検知に効く。', defEn:'Accuracy decay over time as input data and business patterns shift (new formats, new counterparties). It demands continuous monitoring beyond launch accuracy; sampling reviews of high-confidence data help detect it early.',
    aliases:['ドリフト','drift','精度が落ち','パターンが変化'] },
  { key:'fulfillment-center', domain:'agentic', ja:'フルフィルメントセンター', en:'Fulfillment center',
    def:'注文商品の保管・ピッキング・梱包・発送を担う物流拠点。sync_inventory等の在庫同期・注文処理ツールの文脈で登場し、その在庫を更新する操作は実業務に影響するため、権限・ログ・人間承認・冪等性が重要になる。', defEn:'A logistics site that stores, picks, packs, and ships orders. It appears in inventory-sync/order-processing tool scenarios (e.g., sync_inventory), where updating stock affects real operations—demanding permissions, logging, human approval, and idempotency.',
    aliases:['フルフィルメント','fulfillment','物流拠点','在庫同期','sync_inventory'] },
  { key:'multi-pass', domain:'context', ja:'マルチパス・アーキテクチャ', en:'Multi-pass architecture',
    def:'1回のモデル呼び出しで完結させず「抽出→検証→整形」のように複数段階に分ける設計。精度・監査性・QCのしやすさが上がる代わりにレイテンシとコストが増える。リアルタイム性が要る場面ではトレードオフになる。', defEn:'A design that splits work into multiple passes (extract → verify → format) instead of one call, gaining accuracy, auditability, and easier QC at the cost of latency and spend—a real trade-off when responses must be real-time.',
    aliases:['マルチパス','multi-pass','multipass','複数段階','2段階で'] },
  { key:'entity', domain:'context', ja:'エンティティ（Entity）', en:'Entity',
    def:'分析対象になる重要なもの（IPアドレス・ユーザーID・商品ID・拠点・タイムスタンプ等）。大量ログはまずキーとなるエンティティに圧縮抽出し、その後に相関を見ると分析しやすい。生ログ全体をそのまま統合しない。', defEn:'The key items an analysis cares about (IP addresses, user IDs, product IDs, sites, timestamps). Compress raw logs into entities first, then correlate—rather than merging entire raw logs as-is.',
    aliases:['エンティティ','entity','entities'] },
  { key:'correlation-analysis', domain:'context', ja:'相関分析', en:'Correlation analysis',
    def:'別々の出来事・データ間のつながりを見る分析（同一IPが複数拠点で短時間に操作している等）。ファイル横断・拠点横断の攻撃検出で重要で、各ファイルからエンティティを抽出してから相関を見るマルチパス設計と相性が良い。', defEn:'Finding links across separate events/data (e.g., one IP acting at multiple sites within minutes). Central to cross-file, cross-site attack detection, and a natural pair with multi-pass designs: extract entities per file, then correlate.',
    aliases:['相関分析','correlation','相関を見','横断的な関係'] },
  { key:'extended-thinking', domain:'context', ja:'Extended Thinking（拡張思考）', en:'Extended thinking',
    def:'モデルにより多くの推論ステップ・計算余地を使わせる機能。難しい推論には有効だが、大量の生ログを詰め込んで重要な相関が埋もれる問題は「もっと考えさせる」だけでは解決しない。入力の分割・抽出・統合の設計が先。', defEn:'A capability giving the model more reasoning steps. It helps hard reasoning, but when key correlations drown in a stuffed context, thinking harder does not fix input density—split, extract, and integrate the input instead.',
    aliases:['Extended Thinking','拡張思考','extended thinking','thinking budget'] },
  { key:'monorepo', domain:'claudecode', ja:'モノレポ（Monorepo）', en:'Monorepo',
    def:'複数のアプリ・ライブラリを1つのリポジトリで管理する構成（apps/web・apps/api・packages/shared等）。エージェントがコードを調べるときは、API呼び出し元（フロント）と実装先（バックエンド）が別フォルダにあるため両方探す。対象フォルダを間違えない。', defEn:'One repository holding multiple apps/libraries (apps/web, apps/api, packages/shared). When an agent investigates code, the API caller (frontend) and the implementation (backend) live in different folders—search both and don’t confuse targets.',
    aliases:['モノレポ','monorepo','モノリポジトリ','1つのリポジトリ','apps/web'] },
  { key:'http-client', domain:'claudecode', ja:'HTTPクライアント（Axios/Fetch/SWR）', en:'HTTP client (Axios / Fetch / SWR)',
    def:'アプリからHTTPリクエストを送りAPIと通信する仕組み・ライブラリ。axios.get / fetch / useSWR の形でエンドポイントの呼び出し箇所を特定できる。HTTPクライアントは呼ぶ側であり、実処理やDB更新はバックエンド側にあることが多い。', defEn:'The library/mechanism an app uses to call APIs over HTTP. Spot call sites via axios.get / fetch / useSWR; remember the HTTP client is the caller—actual processing and DB writes usually live on the backend.',
    aliases:['HTTPクライアント','Axios','useSWR','HTTP client','axios.get'] },
  { key:'grep', domain:'claudecode', ja:'grep（グレップ）', en:'grep',
    def:'ファイル群から指定した文字列・パターンを検索するコマンド（高速版はrg/ripgrep）。エージェントのコード調査で、エンドポイント名・関数名・エラー文の呼び出し元と実装箇所を探すのに使う。ヒットが定義か呼び出しかテストかは区別して読む。', defEn:'A command that searches files for strings/patterns (ripgrep being the fast variant). Agents use it to find call sites and implementations of endpoints, functions, and error strings; always distinguish whether a hit is a definition, a call, or a test.',
    aliases:['grep','ripgrep','グレップ','文字列検索'] },
  { key:'refactoring', domain:'claudecode', ja:'リファクタリング', en:'Refactoring',
    def:'外から見た動作は変えずに、コードの構造・読みやすさ・保守しやすさを改善すること。バグ修正や新機能追加ではない。Claudeに頼むときも「動作を変えず整理」「既存テストを通す」「無関係な大改造をしない」が要点で、実施後はテストで動作不変を確認する。', defEn:'Improving code structure and readability without changing external behavior—distinct from bug fixes or new features. When delegating to Claude: preserve behavior, keep existing tests passing, avoid unrelated rewrites, and verify with tests afterward.',
    aliases:['リファクタリング','refactor','構造を改善','動作は変えず'] },
  { key:'fork-session', domain:'claudecode', ja:'fork-session（セッション分岐）', en:'Fork session',
    def:'今の会話・作業状態を出発点に、別の分岐セッションを作ること。元の作業を壊さずA案/B案を並行検討できる。分岐後の変更が元セッションへ自動反映されるとは限らず、分岐してもプロジェクトルール・ツール権限・承認ルールは引き続き守る（別物にならない）。', defEn:'Branching a new session from the current context to try alternatives without disturbing the original work. Changes in the fork don’t automatically flow back, and project rules, tool permissions, and approval rules still apply—forking never resets guardrails.',
    aliases:['fork-session','フォークセッション','セッションを分岐','fork'] },
  { key:'tool-description', domain:'tool', ja:'ツール説明（tool description）', en:'Tool description',
    def:'ツール定義の中で「何をし、いつ使うべきで、いつ使うべきでないか」を書く説明文。Claudeはこれを読んでツールを選ぶ。似たツールが並ぶときは「判例なら search_case_law、現行法規なら search_statutes」のように境界線を対比で明示するのが正解。', defEn:'The text in a tool definition stating what the tool does, when to use it, and when not to. Claude selects tools by reading it; with similar tools, spell out the boundary contrastively (“case law → search_case_law; current statutes → search_statutes”).',
    aliases:['ツール説明','tool description','使うべきでない場合','いつ使うか'] },
  { key:'tool-conflict', domain:'tool', ja:'ツール競合（tool conflict）', en:'Tool conflict',
    def:'役割の似たツールが複数あり、Claudeがどれを使うべきか迷って選択がぶれる状態。対策は各descriptionに「使う場合／使わない場合」を対比で書くこと。全ケースで複数ツールを強制実行したり、ルーティング専用ツールを足すのはコスト増・過剰設計になりがち。', defEn:'When overlapping tools make Claude’s selection unstable. The fix is contrastive descriptions (“use when… / don’t use when…”); forcing every tool to run or adding a router tool usually overengineers what is a documentation gap.',
    aliases:['ツール競合','tool conflict','どのツールを使うか迷','似たツール'] },
  { key:'stop-reason', domain:'tool', ja:'stop_reason', en:'stop_reason',
    def:'Claudeの応答がなぜ止まったかを示すフィールド。tool_use=ツール実行要求 / end_turn=ターン完了 / max_tokens=上限で途中停止。ツールループの終了判定は content[0] ではなくこれを見る。', defEn:'Field indicating why the response stopped: tool_use (tool requested), end_turn (turn complete), max_tokens (cut off). Loop termination should check this, not content[0].',
    aliases:['stop_reason','end_turn','ストップリーズン'] },
  { key:'content-block', domain:'tool', ja:'content block（コンテンツブロック）', en:'Content block',
    def:'応答のcontent配列に並ぶ部品（text / tool_use / tool_result）。1つの応答に複数入ることがあり、content[0]だけ見ると後続のtool_useを見落とす。ツールループではstop_reasonと全ブロックを確認する。', defEn:'The parts of a response’s content array (text, tool_use, tool_result). One response can hold several, so checking only content[0] misses later tool_use blocks—inspect stop_reason and all blocks in tool loops.',
    aliases:['content block','コンテンツブロック','content配列','content[0]'] },
  { key:'tool-choice', domain:'tool', ja:'tool_choice', en:'tool_choice',
    def:'ツールを使うかどうかの制御設定。auto=モデルが使用/テキスト回答を判断（デフォルト）、any=必ずどれかのツールを使う強制、tool=特定ツール強制、none=不使用。「予約はツール・挨拶はテキスト」の切り替えはauto。any/tool強制は入力と合わないときパラメータ捏造を招く。', defEn:'Controls tool usage: auto lets the model decide (default), any forces some tool call, tool forces a specific one, none disables tools. Switching between tool calls and plain replies is auto’s job; forcing any/tool on mismatched inputs invites hallucinated parameters.',
    aliases:['tool_choice','ツールチョイス','tool choice','必ずツールを使'] },
  { key:'non-interactive', domain:'claudecode', ja:'非インタラクティブモード', en:'Non-interactive mode',
    def:'人間の対話を待たず、最初に渡した指示だけで実行して結果を出し終了するモード。CI/CDやスクリプトの無人環境ではClaude Codeを -p / --print で実行する。--ci-mode という専用フラグは存在しない（引っかけ）。', defEn:'Running from the initial instruction alone—no human interaction—then exiting. In unattended CI/CD or scripts, run Claude Code with -p / --print; note there is no dedicated --ci-mode flag (a classic trap).',
    aliases:['非インタラクティブ','non-interactive','無人で実行','対話を待たず','--ci-mode'] },
  { key:'hang', domain:'claudecode', ja:'ハングアップ（hang）', en:'Hang',
    def:'処理が終わらず止まったように見える状態。エラー終了ではなく何かを待ち続けていることが多い。CIでClaude Codeがハングする典型原因は、対話モードで起動して入力待ちになること。解決は -p / --print での非インタラクティブ実行。', defEn:'A process that appears stuck—usually waiting on something rather than erroring out. The classic CI cause is launching Claude Code interactively so it waits for input; the fix is non-interactive execution with -p / --print.',
    aliases:['ハング','ハングアップ','hangs indefinitely','hanging','止まったまま'] },
  { key:'print-flag', domain:'claudecode', ja:'-p / --print フラグ', en:'-p / --print flag',
    def:'Claude Codeを非インタラクティブに実行し、応答を標準出力に出して終了させるCLIフラグ（claude -p "..."）。CI/CD・GitHub Actions・スクリプト・自動レビューの基本形。--backgroundは裏で動かすだけで、結果を待つCIステップの解決策ではない。', defEn:'CLI flags that run Claude Code non-interactively, print the response to stdout, and exit (claude -p "…")—the standard for CI/CD, GitHub Actions, scripts, and automated review. --background merely detaches and does not solve a CI step that must await results.',
    aliases:['--print','claude -p','-pフラグ','printフラグ','--background'] },
  { key:'monolith', domain:'claudecode', ja:'モノリス（monolith）', en:'Monolith',
    def:'分けられる機能・責務・ルールが1つの大きな塊にまとまった構造（一枚岩）。常に悪ではないが、Claude Codeでルート CLAUDE.md に全テーマを詰めると、関係ない作業でも毎回読み込まれコンテキスト汚染・保守困難を招く。必要な文脈だけ読む設計が重要。', defEn:'A structure where separable features and rules live in one big lump. Not inherently bad, but stuffing every topic into the root CLAUDE.md loads irrelevant content on every task—context pollution—so design to load only what each task needs.',
    aliases:['モノリス','monolith','一枚岩','1ファイルに全部'] },
  { key:'modularization', domain:'claudecode', ja:'モジュール化（modularization）', en:'Modularization',
    def:'大きなものを役割ごとの再利用できる部品に分けること。Claude Codeルールならルート CLAUDE.md に全部入れず、state.md・testing.md 等テーマ別に分ける。同じ内容を15箇所へコピーするのはモジュール化でなく重複で、1ファイル＋pathsで対象を広げるのが正解。', defEn:'Splitting a large thing into reusable, role-based parts—e.g., theme-specific rule files (state.md, testing.md) instead of one giant CLAUDE.md. Copying identical rules to 15 places is duplication, not modularization; keep one file and widen its paths globs.',
    aliases:['モジュール化','modularization','テーマ別に分け','ルールを分割'] },
  { key:'module-boundary', domain:'claudecode', ja:'モジュールの境界（module boundary）', en:'Module boundary',
    def:'どこまでを1つの部品・責務として扱うかの境目。良い境界は読み込む文脈を減らし、変更の影響範囲を狭める。細かく分けすぎると探しにくく重複も増える。「とにかく細分化」ではなく、再利用できる単位・変更理由が同じ単位で切る。', defEn:'The line defining what counts as one part/responsibility. Good boundaries shrink loaded context and the blast radius of changes; over-splitting hurts findability and breeds duplication. Cut along reusable units that change for the same reason.',
    aliases:['モジュールの境界','module boundary','境界を決め','責務の区切り'] },
  { key:'attention-decay', domain:'context', ja:'注意力の減衰（attention decay）', en:'Attention decay',
    def:'コンテキストに入ってはいても、情報量が多すぎるとモデルが重要な細部や遠く離れた関係に十分注意できなくなる現象。「入る」と「見つけられる」は別。より大きいウィンドウやmax_tokens増加では入力密度の問題は解決せず、局所分析＋統合フェーズに分ける。', defEn:'Even when everything fits in context, excessive volume keeps the model from attending to key details and distant relations—fitting is not finding. Bigger windows or higher max_tokens don’t fix input density; split into local analyses plus an integration phase.',
    aliases:['注意力の減衰','attention decay','注意が薄ま','重要な細部を見落と'] },
  { key:'integration-phase', domain:'context', ja:'統合フェーズ（integration phase）', en:'Integration phase',
    def:'分割して抽出した分析結果を集め、全体の関係・相関・境界候補を判断する最終段階。並列の局所分析だけでは全体判断は終わっておらず、ファイル横断・チャンク横断の関係はここで見る。「分割するだけ」で止めず、抽出結果を構造化して統合するまで設計する。', defEn:'The final stage that gathers per-chunk extractions and judges cross-cutting relations and boundaries. Parallel local analysis alone leaves the global question unanswered—design through to structuring and integrating the results, not just splitting.',
    aliases:['統合フェーズ','integration phase','統合する段階','結果を統合'] },
  { key:'review-instance', domain:'agentic', ja:'レビューインスタンス（review instance）', en:'Review instance',
    def:'最初に答えを作ったClaudeとは別に、その答えを点検・批判・改善提案する別のClaude呼び出し。品質チェックには役立つが万能ではない。元の分析が大量入力で重要情報を見落としていれば、同じ材料ではレビュー側も見抜けない。入力密度の整理が先。', defEn:'A separate Claude call that critiques the first Claude’s answer. Useful for quality checks but not a cure-all: if the original analysis drowned key facts in bulk input, a reviewer fed the same material misses them too—fix input density first.',
    aliases:['レビューインスタンス','review instance','別のClaudeにレビュー','レビュー役の'] },
  { key:'redis-cache', domain:'context', ja:'Redisキャッシュ', en:'Redis cache',
    def:'高速なインメモリデータストアRedisに、よく使うデータや重い検索結果を一時保存して速く返す仕組み。レイテンシ削減・外部API/DB負荷軽減に効くが、本体DBの代わりではない。古いデータを返すリスクがあるためTTL・無効化・再生成の設計が必須（入れれば正確になる、は誤り）。', defEn:'Caching hot data or heavy query results in Redis, a fast in-memory store, to cut latency and offload the database or external APIs. Not a substitute for the primary DB: stale data is a real risk, so TTLs, invalidation, and regeneration must be designed—adding Redis does not make answers more correct.',
    aliases:['Redis','キャッシュ層','in-memory cache','インメモリ'] },
  { key:'ls-recursive', domain:'claudecode', ja:'ls -R（再帰一覧）', en:'ls -R (recursive listing)',
    def:'フォルダ配下をサブフォルダまで再帰的に一覧表示するコマンド。プロジェクト構造の把握に使うが、巨大リポジトリで実行すると出力が多すぎてコンテキストを汚す。構造を見るだけで本文は読まない（本文はcat、検索はgrep/rg）。', defEn:'Lists a directory tree recursively. Handy for grasping project structure, but on a huge repo the output floods context. It shows structure only—use cat for contents and grep/rg for search.',
    aliases:['ls -R','ディレクトリ構造を一覧','再帰的に一覧'] },
  { key:'cat-command', domain:'claudecode', ja:'cat（ファイル表示）', en:'cat (print file contents)',
    def:'ファイルの中身をそのまま表示するコマンド（元は連結の意）。大きいファイルを丸ごと出すとコンテキストを圧迫するため範囲を絞る方がよい。catは検索ではない: 特定語を探すならgrep/rg、構造を見るならls。', defEn:'Prints a file’s contents (originally “concatenate”). Dumping huge files wastes context, so narrow the range. cat is not search—use grep/rg to find terms and ls for structure.',
    aliases:['cat ','ファイルの中身を表示','丸ごと表示'] },
  { key:'go-backend', domain:'claudecode', ja:'Goバックエンド', en:'Go backend',
    def:'Go言語で書かれたサーバー側アプリ（APIサーバー等）。コード調査ではルート定義・ハンドラ・サービス層・DBクエリを見る。SQLインジェクションや認証不備はフロントではなくバックエンド側の入力処理・DB処理で成立することが多い。', defEn:'A server-side application written in Go (API server etc.). Code investigations look at route definitions, handlers, service layers, and DB queries; SQL injection and auth flaws usually materialize in backend input/DB handling, not the frontend.',
    aliases:['Goバックエンド','Go backend','Golang','Goで書かれた'] },
  { key:'scratchpad-file', domain:'claudecode', ja:'スクラッチパッドファイル', en:'Scratchpad file',
    def:'作業中の考え・調査メモ・途中結果を一時的に書き出すファイル。長い調査やリファクタリングでClaudeが途中の発見や計画を失わないために使う。最終成果物ではないので納品文書とは分け、秘密情報を雑に残さない。', defEn:'A temporary file for in-progress notes, findings, and plans so long investigations don’t lose state. It is not a deliverable—keep it separate from final docs and never park secrets in it casually.',
    aliases:['スクラッチパッド','scratchpad','作業メモファイル','一時メモ'] },
  /* ===== 2026-07-07 追加（演習1-11・PySpark/評価/位置効果 ほか） ===== */
  { key:'lost-in-the-middle', domain:'context', ja:'位置効果（Lost in the middle）', en:'Lost in the middle (positional effect)',
    def:'長いコンテキストで、冒頭と末尾の情報は拾えても真ん中付近を見落としやすい位置効果。分割（チャンキング）が効くが、厳しいレイテンシ要件等で分割できないときは、重要点の要約を冒頭に置き、本文をページ・期間・種別など明確な見出しで構造化して補う。注意力の減衰（attention decay）の位置版。', defEn:'The positional effect where a model catches information at the start and end of a long context but misses what sits in the middle. Chunking helps, but when latency rules it out, put a summary of key findings up front and structure the body with clear headings (page/period/type). It is the positional variant of attention decay.',
    aliases:['位置効果','lost in the middle','真ん中を見落と','中盤の情報','ロスト・イン・ザ・ミドル'] },
  { key:'context-rot', domain:'context', ja:'コンテキストの劣化（context rot）', en:'Context rot / degradation',
    def:'会話やコンテキストが長くなるほど、古い指示や重要情報が埋もれ・薄まり、応答品質が落ちていく現象。要約・再掲・圧縮（compaction）・重要指示の末尾再提示などで劣化を抑える。長ければ長いほど良い、ではない。', defEn:'As a conversation or context grows, older instructions and key facts get buried and diluted, degrading response quality. Counter it with summarization, restating, compaction, and re-surfacing critical instructions near the end. Longer context is not always better.',
    aliases:['コンテキストの劣化','context rot','文脈が劣化','情報が薄ま','長くなると精度が落ち'] },
  { key:'transaction-log', domain:'context', ja:'トランザクションログ', en:'Transaction log',
    def:'取引・処理を時系列で記録した長大なログ（財務監査・決済・DB等）。一度に全部渡すと位置効果で中盤の異常を見落としやすい。冒頭に要点要約、本文はページ・期間・取引種別の見出しで構造化して渡すのが定石。', defEn:'A long, time-ordered record of transactions (financial audit, payments, DBs). Handing it over all at once invites lost-in-the-middle misses in the middle sections; lead with a summary and structure the body by page, period, and transaction type.',
    aliases:['トランザクションログ','transaction log','取引ログ','監査ログ','財務監査'] },
  { key:'data-skew', domain:'context', ja:'データスキュー（偏り）', en:'Data skew',
    def:'一部のパーティション（区画）にデータが極端に偏る状態。誤ったパーティションキーやウィンドウ関数の設計で起き、下流のgroupByで特定ノードに負荷が集中しOOM（メモリ不足）を招く。分散処理で頻出の因果の起点。', defEn:'When data piles up unevenly across a few partitions. A wrong partition key or window-function design causes it, and a downstream groupBy then overloads specific nodes into OOM. It is the starting cause in the classic PySpark failure chain.',
    aliases:['データスキュー','skew','スキュー','偏ったパーティション','データが偏'] },
  { key:'partitioning', domain:'context', ja:'パーティショニング（分割キー）', en:'Partitioning',
    def:'大きなデータを分割キーで区画（パーティション）に分けて分散処理する仕組み。キー選びが悪いと特定区画にデータが偏る（スキュー）。ウィンドウ関数やgroupByの土台。', defEn:'Splitting large data into partitions by a key so it can be processed in parallel. A poor key skews data into a few partitions. It underpins window functions and groupBy.',
    aliases:['パーティショニング','partitioning','partition','分割キー','区画に分け'] },
  { key:'window-function', domain:'context', ja:'ウィンドウ関数', en:'Window function',
    def:'行のまとまり（ウィンドウ）ごとに集計・順位付け等を行うSQL/分散処理の関数。誤ったキーでパーティショニングするとスキューの起点になる。PySparkの因果連鎖でよく登場する。', defEn:'A SQL/distributed function that aggregates or ranks over a set of rows (a window). Partitioning it on the wrong key seeds skew. A recurring term in the PySpark causal chain.',
    aliases:['ウィンドウ関数','window function','partition by','行のまとまりごと'] },
  { key:'groupby', domain:'context', ja:'groupBy（グループ集計）', en:'groupBy',
    def:'キーごとにデータをまとめて集計する操作。上流でスキューした出力に依存すると、偏った大きなデータを一部のエグゼキュータが抱えてOOMになる。原因（誤キー→スキュー）と症状（groupByでOOM）を一続きで捉える。', defEn:'Aggregates data by key. When it depends on skewed upstream output, a few executors hold the oversized, lopsided data and hit OOM. Read cause (bad key to skew) and symptom (OOM at groupBy) as one chain.',
    aliases:['groupBy','.groupBy()','グループ集計','キーごとに集計'] },
  { key:'executor-node', domain:'context', ja:'エグゼキュータノード', en:'Executor node',
    def:'分散処理（Spark等）で実際に計算を担う実行ノード。スキューで特定ノードに巨大データが集中すると、そのノードのメモリを食い潰してOOMになる。処理は分散していても、偏れば1ノードが落ちる。', defEn:'A worker node that actually runs computation in distributed systems (e.g., Spark). When skew concentrates huge data on one node, it exhausts that node’s memory into OOM. Even distributed work fails at a single node if it is lopsided.',
    aliases:['エグゼキュータ','executor','実行ノード','ワーカーノード'] },
  { key:'oom', domain:'context', ja:'OOM（メモリ不足）', en:'OOM (out of memory)',
    def:'処理に必要なメモリが足りず落ちる状態。PySpark問題では、誤パーティションキー→データスキュー→groupByで一部ノードに集中、という因果の終点。Claudeに頼むときは「メモリを増やせば良い」で終わらせず根本原因（スキュー）まで一続きで伝える。', defEn:'A crash from running out of the memory a job needs. In the PySpark scenario it is the end of the chain: wrong partition key to data skew to concentration at a few nodes on groupBy. When asking Claude, do not stop at “just add memory”—convey the root cause (skew) as one chain.',
    aliases:['OOM','out of memory','メモリ不足','メモリを食い潰'] },
  { key:'pyspark', domain:'context', ja:'PySpark（分散データ処理）', en:'PySpark',
    def:'Pythonから大規模データを分散処理するApache Sparkのインターフェース。パーティショニング・ウィンドウ関数・groupByでデータを扱う。バグが連鎖しやすく、Claudeには「誤キー→スキュー→OOM」のように原因から下流影響まで1メッセージで説明するのが効く。', defEn:'The Python interface to Apache Spark for large-scale distributed data processing, using partitioning, window functions, and groupBy. Bugs chain easily, so explain them to Claude as one message from cause to downstream impact (bad key to skew to OOM).',
    aliases:['PySpark','pyspark','PySparkジョブ','Spark','分散データ処理'] },
  { key:'imbalanced-data', domain:'context', ja:'不均衡データ', en:'Imbalanced data',
    def:'カテゴリやセグメントごとの件数が大きく偏っているデータ。多数派が精度を押し上げ、少数派（高リスク）で失敗していても全体精度は高く見える。全体の数字だけで判断せず、少数セグメントを切り出して独立に評価する。', defEn:'Data whose categories or segments differ greatly in volume. The majority inflates accuracy so failures in a small, high-risk minority stay hidden behind a high overall number. Do not judge on the aggregate—carve out minority segments and evaluate them independently.',
    aliases:['不均衡データ','imbalanced','偏ったデータ','多数派が押し上げ'] },
  { key:'segment-evaluation', domain:'context', ja:'セグメント別評価（少数高リスクの独立評価）', en:'Segment-level evaluation',
    def:'全体精度ではなく、リスクや属性ごとのセグメントに分けて精度を測る評価。全体98%でも2%の特殊検査で失敗している可能性がある。医療・金融・法務など高リスク領域は、少数でも切り出して統計的に有意なサンプルで独立評価する。', defEn:'Measuring accuracy per segment (by risk or attribute) instead of overall. A 98% aggregate can still hide failures in a 2% specialized slice. High-risk domains (medicine, finance, law) need small segments carved out and evaluated independently on a statistically significant sample.',
    aliases:['セグメント評価','セグメント別','少数高リスク','切り出して評価','独立して評価'] },
  { key:'field-level-accuracy', domain:'context', ja:'フィールドレベルの精度', en:'Field-level accuracy',
    def:'出力全体の正誤ではなく、項目（診断名・検査値・変異・リスク分類など）ごとに正しいかを測る精度。医療など高リスク領域では1項目の誤りが重大になるため、項目単位で評価する。', defEn:'Accuracy measured per field (diagnosis name, test value, variant, risk class) rather than whole-output correctness. In high-risk domains like medicine a single wrong field can be critical, so evaluate item by item.',
    aliases:['フィールドレベル','field-level','項目ごとの精度','項目単位'] },
  { key:'statistical-significance', domain:'context', ja:'統計的に有意', en:'Statistical significance',
    def:'観測された差や精度が偶然では説明しにくい、と言えるだけの十分なサンプル量・確からしさがある状態。少数セグメントを独立評価するときは、結論を出せるだけの有意なサンプルサイズを確保する。数件だけ見て判断しない。', defEn:'Having enough sample and confidence that an observed difference or accuracy is unlikely to be chance. When evaluating a minority segment independently, secure a statistically significant sample before concluding—do not judge from a handful of cases.',
    aliases:['統計的に有意','statistically significant','有意なサンプル','サンプルサイズ'] },
  { key:'causal-chain', domain:'prompt', ja:'因果連鎖で伝える（複数バグを1メッセージで）', en:'Causal chain (describe linked bugs together)',
    def:'複数の不具合が原因→症状でつながっているとき、症状ごとに別々に投げず、原因から下流影響までを1つの詳細なメッセージでClaudeに渡すプロンプト設計。例: 誤パーティションキー→スキュー→groupByでOOM。症状だけ・原因だけの分割投げは根本原因を落とす。', defEn:'When several defects are linked cause-to-symptom, do not send symptoms separately—give Claude one detailed message spanning the root cause to downstream impact (e.g., wrong partition key to skew to OOM at groupBy). Splitting into symptom-only or cause-only prompts drops the root cause.',
    aliases:['因果連鎖','causal chain','原因から下流','1つのメッセージで説明','まとめて説明'] },
  { key:'base64', domain:'tool', ja:'Base64エンコード', en:'Base64 encoding',
    def:'画像・PDF・音声などのバイナリを、テキスト（ASCII文字列）として安全に運ぶための符号化方式。Claude APIに画像やファイルをJSONで渡すときに使う。中身を暗号化するものではない（誰でもデコード可能）ので、機密保護の手段ではない。', defEn:'An encoding that carries binary data (images, PDFs, audio) as ASCII text so it can travel safely in JSON—used when passing images/files to the Claude API. It is not encryption (anyone can decode it), so it is not a confidentiality measure.',
    aliases:['base64','Base64','バイナリをテキスト','画像をエンコード'] },
  { key:'escalation', domain:'agentic', ja:'エスカレーション（人間への引き継ぎ）', en:'Escalation (hand off to a human)',
    def:'AIや一次対応で処理しきれない・してはいけないケースを人間へ引き継ぐこと。ユーザーが「責任者に代わって」「人間に繋いで」と明示要求したら、ツールで解決できそうでも即時エスカレーションを優先する。ポリシーが沈黙する高リスク事象も人間へ。「ルールがあるなら必ず拒否」ではない。', defEn:'Handing a case to a human when AI or first-line handling cannot or should not resolve it. If a user explicitly asks for a supervisor or a human, escalate immediately even if a tool could do the job. Also escalate high-risk cases where policy is silent. It is not “always refuse when a rule exists.”',
    aliases:['エスカレーション','escalation','人間に繋','責任者に代わ','人間へ引き継'] }
];
